[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"
  NODE_VERSION = "20"

# 1. Admin Panel Redirect
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200

# 2. SPA Fallback (Catch-all)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
